 public function index(){

      //fazendo o update de um registro
      //nesse caso o eloquent vai fazer o update
      //conforme o contexto se eu selecionei produto 1 
      //qualquer update que eu fizer vai ser no produto 1
      //as linhas abaixo vão pegando os dados por contexto 

      //update para um unico produto
      // $product = Product::find(1);
      // $product->product_name = 'Produto Atualizado'; 
      // $product->price= 57;
      // $product->save();
      
      //update para varios produtos
      // Product::where('price','>=', 10)
      //            ->update(['price' => 100]); 
      //
      //

      //atualizar se existir ou criar se não existir
      //isso pode ser util para cirra um usuario paradrão no sistema 
      //para que um administrador possa acessar o sistemae criar novos usuarios
      //mas aui estou editando o preço da batata se ela não existir essa função vai gerara oi cadastro e aplicar 
      //o preço  
      // Product::updateOrCreate(
      //     ['product_name' => 'Batata'],
      //     ['price' => 100]
      // );

      //deletar um produto
      // $product = Product::find(2);
      // $product->delete();

      //eliminar todos os registros da tabela de produtos
      //e reseta o contador de auto incremento
      // Product::truncate();  
      
      //para usar o sotft delete
      //é necessário adicionar o trait SoftDeletes no model Product
      // Product::find(1)->delete(); //isso vai deletar o produto mas não
      //vai eliminar o registro do banco de dados
      //no caso seir fazer um update preencehndo o campo sioft delete e avisar sistema
      //que qualquer registro que tiver uma adata no campo deleted_at
      //não deve ser exibido na listagem de produtos
      
      //para restaurar um produto que foi deletado
      //existe uma fotm a mais simples de restaurar
      // Product::withTrashed()->find(1)->restore();
      //mas eu prefiro usar essa porque fica mais claro pra mim 

      // $product = Product::withTrashed()->find(1);
      // $product->restore();

      
         
      
   }